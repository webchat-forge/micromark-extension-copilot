import React from 'react';

export const Header: React.FC = () => (
  <div className="playground-header">
    <h1>Math Extension Playground</h1>
    <p>Try out different math notations using the editor below</p>
  </div>
);